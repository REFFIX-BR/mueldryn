<section class="content-header">
    <h1>Rifas <a href="<?= url_to('/admin/raffles/add') ?>" class="btn btn-sm btn-primary pull-right">Adicionar Rifa</a></h1>
</section>

<section class="content">
    <div class="box">
        <div class="box-body no-padding">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nome</th>
                        <th>Números</th>
                        <th>Preço</th>
                        <th>Status</th>
                        <th width="150">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($raffles)): ?>
                        <tr>
                            <td colspan="6" class="text-center">Nenhuma rifa cadastrada.</td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($raffles as $raffle): ?>
                            <tr>
                                <td><?= $raffle['id'] ?></td>
                                <td><?= htmlspecialchars($raffle['name']) ?></td>
                                <td><?= $raffle['total_numbers'] ?></td>
                                <td><?= $raffle['price'] ?></td>
                                <td>
                                    <?php if (!empty($raffle['winner'])): ?>
                                        <span class="label label-info">Sorteada (Nº <?= $raffle['winner_number'] ?> - <?= htmlspecialchars($raffle['winner']) ?>)</span>
                                    <?php elseif ($raffle['active']): ?>
                                        <span class="label label-success">Ativa</span>
                                    <?php else: ?>
                                        <span class="label label-danger">Inativa</span>
                                    <?php endif; ?>
                                </td>
                                <td>
                                    <?php if (empty($raffle['winner'])): ?>
                                        <a href="<?= url_to('/admin/raffles/draw/' . $raffle['id']) ?>" class="btn btn-xs btn-warning" data-ajax-action data-confirm="Deseja realizar o sorteio agora? O ganhador será escolhido entre os números comprados.">Sortear</a>
                                    <?php endif; ?>
                                    <a href="<?= url_to('/admin/raffles/edit/' . $raffle['id']) ?>" class="btn btn-xs btn-default">Editar</a>
                                    <a href="<?= url_to('/admin/raffles/delete/' . $raffle['id']) ?>" class="btn btn-xs btn-danger" data-ajax-action data-confirm="Tem certeza que deseja excluir?">Excluir</a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</section>