<div class="row">
    <div class="col-md-12">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Mercado de Personagens</h3>
            </div>
            <div class="box-body">
                <table class="table table-striped table-hover data-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Vendedor</th>
                            <th>Personagem</th>
                            <th>Preço</th>
                            <th>Data do Anúncio</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($chars as $char): ?>
                            <tr>
                                <td><?= $char['id'] ?></td>
                                <td><?= $char['seller'] ?></td>
                                <td><strong><?= $char['character_name'] ?></strong></td>
                                <td>
                                    <?= number_format($char['price'], 0, ',', '.') ?> 
                                    <?= isset($coins[$char['coin_id']]) ? $coins[$char['coin_id']]['name'] : 'Moeda Excluída' ?>
                                </td>
                                <td><?= date('d/m/Y H:i', strtotime($char['created_at'])) ?></td>
                                <td>
                                    <form action="/admin/marketchars/delete/<?= $char['id'] ?>" method="POST" class="ajax-form" style="display:inline;">
                                        <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Tem certeza que deseja excluir este anúncio e devolver o personagem ao vendedor?');"><i class="fa fa-trash"></i> Excluir</button>
                                    </form>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
